@model IEnumerable<Hotel_Dejvid.Models.Soba>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrojSobe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipSobe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CenaPoNoci)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dostupna)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrojSobe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipSobe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CenaPoNoci)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dostupna)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SobaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SobaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SobaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
