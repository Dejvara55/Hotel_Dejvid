@model IEnumerable<Hotel_Dejvid.Models.Placanje>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RezervacijaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Iznos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumPlacanje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VrstaPlacanja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RezervacijaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Iznos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumPlacanje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VrstaPlacanja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlacanjeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlacanjeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlacanjeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
